from datetime import datetime\nfrom flask_sqlalchemy import SQLAlchemy\nfrom flask_bcrypt import Bcrypt\n\ndb = SQLAlchemy()\nbcrypt = Bcrypt()\n\n# User Model\nclass User(db.Model):\n    id = db.Column(db.Integer, primary_key=True)\n    username = db.Column(db.String(150), unique=True, nullable=False)\n    email = db.Column(db.String(150), unique=True, nullable=False)\n    password = db.Column(db.String(128), nullable=False)\n    user_progress = db.relationship('UserProgress', backref='user', lazy=True)\n    submission_history = db.relationship('SubmissionHistory', backref='user', lazy=True)\n\n# DSA Problem Model\nclass DSAProblem(db.Model):\n    id = db.Column(db.Integer, primary_key=True)\n    title = db.Column(db.String(200), nullable=False)\n    pattern = db.Column(db.String(100), nullable=False)\n    difficulty = db.Column(db.String(50), nullable=False)\n    templates = db.Column(db.Text, nullable=True)\n    test_cases = db.relationship('TestCase', backref='problem', lazy=True)\n\nclass TestCase(db.Model):\n    id = db.Column(db.Integer, primary_key=True)\n    input_data = db.Column(db.Text, nullable=False)\n    expected_output = db.Column(db.Text, nullable=False)\n    problem_id = db.Column(db.Integer, db.ForeignKey('dsa_problem.id'), nullable=False)\n\n# Contest Model\nclass Contest(db.Model):\n    id = db.Column(db.Integer, primary_key=True)\n    name = db.Column(db.String(200), nullable=False)\n    start_time = db.Column(db.DateTime, nullable=False)\n    end_time = db.Column(db.DateTime, nullable=False)\n    participants = db.relationship('User', secondary='contest_participants', lazy='subquery')\n\ncontest_participants = db.Table('contest_participants',\n    db.Column('contest_id', db.Integer, db.ForeignKey('contest.id')),\n    db.Column('user_id', db.Integer, db.ForeignKey('user.id'))\n)\n\n# Leaderboard Model\nclass Leaderboard(db.Model):\n    id = db.Column(db.Integer, primary_key=True)\n    user_id = db.Column(db.Integer, db.ForeignKey('user.id'))\n    score = db.Column(db.Integer, default=0)\n    user = db.relationship('User', backref='leaderboard_entry')\n\n# User Progress Model\nclass UserProgress(db.Model):\n    id = db.Column(db.Integer, primary_key=True)\n    user_id = db.Column(db.Integer, db.ForeignKey('user.id'))\n    streak = db.Column(db.Integer, default=0)\n    mastery_level = db.Column(db.Integer, default=0)\n\n# Point System Model\nclass PointSystem(db.Model):\n    id = db.Column(db.Integer, primary_key=True)\n    user_id = db.Column(db.Integer, db.ForeignKey('user.id'))\n    points = db.Column(db.Integer, default=0)\n    problem_id = db.Column(db.Integer, db.ForeignKey('dsa_problem.id'))\n    time_taken = db.Column(db.Float)  # Time in seconds\n\n# Submission History Model\nclass SubmissionHistory(db.Model):\n    id = db.Column(db.Integer, primary_key=True)\n    user_id = db.Column(db.Integer, db.ForeignKey('user.id'))\n    problem_id = db.Column(db.Integer, db.ForeignKey('dsa_problem.id'))\n    submitted_code = db.Column(db.Text, nullable=False)\n    execution_result = db.Column(db.Text, nullable=False)\n    submission_time = db.Column(db.DateTime, default=datetime.utcnow)\n